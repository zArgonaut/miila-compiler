int m[][] = {{1, 2, 3}, {4, 5, 6}};
mout(m[0][0]);
mout(m[1][2]);
m[0][1] = 99;
mout(m[0][1]);
mout(len(m));
int t[2][2];
for i = 0; i < 2; i++ {
    for j = 0; j < 2; j++ {
        t[i][j] = i * 2 + j;
    }
}
mout(t[1][1]);
